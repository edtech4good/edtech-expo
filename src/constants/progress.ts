// Client-side pass mark for practices/quizzes. Mirrors the server's
// COMPLETED_PERCENTAGE (edtech-lms-api / edtech-lms-rpi-api) — keep the two
// in sync if either changes.
export const PASS_PERCENTAGE = 80;
